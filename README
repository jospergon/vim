This is my personal settings for gvim.

This configuration uses Vundle (https://github.com/gmarik/vundle) for installing de plugins.

INSTALL
=======
Copy .vimrc into your HOME directory.
After launch vim and run :BundleInstall, it will install all the plugins setted in the 
.vimrc file.
